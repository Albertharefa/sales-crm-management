import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { apiDelete, apiGet, apiPost } from "@/lib/api";
import type { Customer, Paginated } from "@/lib/types";
import PageHeader from "@/components/PageHeader";
import Modal from "@/components/Modal";
import { Field, selectClass } from "@/components/Field";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { toast } from "sonner";
import { Search, Trash2 } from "lucide-react";

export default function Customers() {
  const [search, setSearch] = useState(""); const [modal, setModal] = useState(false); const [page, setPage] = useState(1); const [form, setForm] = useState({ name: "", industry: "Manufacturing", city: "Jakarta", phone: "", email: "", pic_name: "", status: "Active", address: "", notes: "" }); const qc = useQueryClient();
  const query = useQuery({ queryKey: ["customers", page, search], queryFn: () => apiGet<Paginated<Customer>>(`/customers?page=${page}&page_size=10&search=${encodeURIComponent(search)}`) });
  const create = useMutation({ mutationFn: () => apiPost<Customer>("/customers", { ...form, email: form.email || null }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["customers"] }); setModal(false); setForm({ name: "", industry: "Manufacturing", city: "Jakarta", phone: "", email: "", pic_name: "", status: "Active", address: "", notes: "" }); toast.success("Customer berhasil ditambahkan"); }, onError: () => toast.error("Customer gagal disimpan") });
  const remove = useMutation({ mutationFn: (id: string) => apiDelete<void>(`/customers/${id}`), onSuccess: () => { qc.invalidateQueries({ queryKey: ["customers"] }); toast.success("Customer dihapus"); } });
  return <div data-testid="customers-page"><PageHeader title="Customers" description="Database pelanggan — pencarian dan filter dijalankan di server" action={{ label: "Tambah Customer", onClick: () => setModal(true) }} onRefresh={() => void query.refetch()} onExport={() => window.open(`/api/customers?export=csv`, "_blank")} /><div className="mb-5 flex flex-col gap-3 sm:flex-row"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-2.5 size-4 text-slate-400" /><Input value={search} onChange={e => { setSearch(e.target.value); setPage(1); }} placeholder="Cari nama, PIC, email..." className="pl-9" data-testid="customers-search-input" /></div><select className={selectClass} data-testid="customers-status-filter"><option>Semua status</option><option>Active</option><option>Prospect</option><option>Inactive</option></select><select className={selectClass} data-testid="customers-industry-filter"><option>Semua industri</option>{["Manufacturing", "Oil & Gas", "Mining", "EPC", "Power Generation"].map(v => <option key={v}>{v}</option>)}</select></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-[11px] uppercase tracking-wider text-slate-500"><tr>{["Customer ID", "Nama Customer", "Industri", "Kota", "PIC", "Sales", "Status", "Aksi"].map(h => <th key={h} className="px-4 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{query.isLoading ? <tr><td colSpan={8} className="px-4 py-10 text-center text-slate-400" data-testid="customers-loading-state">Memuat customer...</td></tr> : query.data?.items.map(customer => <tr key={customer.id} className="hover:bg-slate-50" data-testid={`customer-row-${customer.customer_id}`}><td className="px-4 py-3 font-mono text-xs text-blue-600">{customer.customer_id}</td><td className="px-4 py-3 font-medium">{customer.name}</td><td className="px-4 py-3 text-slate-600">{customer.industry}</td><td className="px-4 py-3 text-slate-600">{customer.city}</td><td className="px-4 py-3">{customer.pic_name ?? "—"}</td><td className="px-4 py-3">{customer.sales_name ?? "—"}</td><td className="px-4 py-3"><Badge variant={customer.status === "Active" ? "default" : "secondary"}>{customer.status}</Badge></td><td className="px-4 py-3"><Button variant="ghost" size="icon-sm" onClick={() => remove.mutate(customer.id)} data-testid={`customer-delete-${customer.customer_id}`}><Trash2 className="size-4 text-red-500" /></Button></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-slate-200 px-4 py-3 text-xs text-slate-500"><span data-testid="customers-pagination-summary">Menampilkan {query.data?.items.length ?? 0} dari {query.data?.total ?? 0} data</span><div className="flex items-center gap-2"><Button variant="outline" size="sm" disabled={page === 1} onClick={() => setPage(page - 1)} data-testid="customers-previous-page">Sebelumnya</Button><span data-testid="customers-current-page">{page}</span><Button variant="outline" size="sm" disabled={!query.data || page * 10 >= query.data.total} onClick={() => setPage(page + 1)} data-testid="customers-next-page">Berikutnya</Button></div></div></div>{modal && <Modal title="Tambah Customer" onClose={() => setModal(false)}><form className="grid gap-4 sm:grid-cols-2" onSubmit={e => { e.preventDefault(); create.mutate(); }} data-testid="customer-create-form"><Field label="Nama Customer" required><Input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} data-testid="customer-name-input" /></Field><Field label="Industri"><select className={selectClass} value={form.industry} onChange={e => setForm({ ...form, industry: e.target.value })} data-testid="customer-industry-input"><option>Manufacturing</option><option>Oil & Gas</option><option>Mining</option><option>EPC</option><option>Power Generation</option></select></Field><Field label="Kota"><Input value={form.city} onChange={e => setForm({ ...form, city: e.target.value })} data-testid="customer-city-input" /></Field><Field label="Telepon"><Input value={form.phone} onChange={e => setForm({ ...form, phone: e.target.value })} data-testid="customer-phone-input" /></Field><Field label="Email"><Input type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} data-testid="customer-email-input" /></Field><Field label="Nama PIC"><Input value={form.pic_name} onChange={e => setForm({ ...form, pic_name: e.target.value })} data-testid="customer-pic-input" /></Field><Field label="Status"><select className={selectClass} value={form.status} onChange={e => setForm({ ...form, status: e.target.value })} data-testid="customer-status-input"><option>Active</option><option>Prospect</option><option>Inactive</option></select></Field><div className="sm:col-span-2"><Field label="Alamat"><Textarea value={form.address} onChange={e => setForm({ ...form, address: e.target.value })} data-testid="customer-address-input" /></Field></div><div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={() => setModal(false)} data-testid="customer-cancel-button">Batal</Button><Button type="submit" disabled={create.isPending} data-testid="customer-save-button">{create.isPending ? "Menyimpan..." : "Simpan"}</Button></div></form></Modal>}</div>;
}