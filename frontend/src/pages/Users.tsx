import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { apiGet, apiPost } from "@/lib/api";
import type { Paginated, User } from "@/lib/types";
import PageHeader from "@/components/PageHeader";
import Modal from "@/components/Modal";
import DataTable from "@/components/DataTable";
import { Field, selectClass } from "@/components/Field";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { toast } from "sonner";

export default function Users() {
  const [modal, setModal] = useState(false);
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(25);
  const [form, setForm] = useState({ name: "", email: "", role: "SALES", manager_id: "", phone: "", status: "Active", password: "Password123" });
  const qc = useQueryClient();
  const query = useQuery({ queryKey: ["users", page, pageSize], queryFn: () => apiGet<Paginated<User>>(`/users?page=${page}&page_size=${pageSize}`) });
  const create = useMutation({ mutationFn: () => apiPost<User>("/users", { ...form, manager_id: form.manager_id || null }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["users"] }); setModal(false); toast.success("User berhasil ditambahkan"); }, onError: () => toast.error("User gagal disimpan") });
  const handleAction = (action: string, user: User) => {
    toast.info(`${action} untuk ${user.name} akan kita aktifkan pada step berikutnya.`);
  };
  return <div data-testid="users-page"><PageHeader title="Kelola Pengguna" description="User_ID adalah identifier utama; Manager_ID menghubungkan hierarki" action={{ label: "Tambah User", onClick: () => setModal(true) }} onRefresh={() => void query.refetch()} /><DataTable testId="users-table" items={query.data?.items ?? []} loading={query.isLoading} total={query.data?.total} page={page} pageSize={pageSize} onPage={setPage} onPageSize={size => { setPageSize(size); setPage(1); }} columns={[{ key: "id", label: "User ID", render: i => <span className="font-mono text-xs text-blue-600">{i.user_id}</span> }, { key: "name", label: "Nama", render: i => <span className="font-medium">{i.name}</span> }, { key: "email", label: "Email", render: i => i.email }, { key: "role", label: "Role", render: i => <Badge>{i.role}</Badge> }, { key: "manager", label: "Manager", render: i => i.manager_id ?? "—" }, { key: "phone", label: "Telepon", render: i => i.phone ?? "—" }, { key: "status", label: "Status", render: i => <Badge variant="outline">{i.status}</Badge> }, { key: "last", label: "Last Login", render: i => i.last_login ? new Date(i.last_login).toLocaleString("id-ID") : "Belum pernah" }, { key: "actions", label: "Aksi", render: i => <div className="flex items-center gap-1"><Button type="button" variant="outline" size="sm" onClick={() => handleAction("View", i)} title="View" aria-label={`View ${i.name}`}>View</Button><Button type="button" variant="outline" size="sm" onClick={() => handleAction("Edit", i)} title="Edit" aria-label={`Edit ${i.name}`}>Edit</Button><Button type="button" variant="outline" size="sm" onClick={() => handleAction("Delete", i)} title="Delete" aria-label={`Delete ${i.name}`}>Delete</Button></div> }]} />{modal && <Modal title="Tambah User" onClose={() => setModal(false)}><form className="grid gap-4 sm:grid-cols-2" onSubmit={e => { e.preventDefault(); create.mutate(); }} data-testid="user-create-form"><Field label="Nama" required><Input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} data-testid="user-name-input" /></Field><Field label="Email" required><Input type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} data-testid="user-email-input" /></Field><Field label="Role"><select className={selectClass} value={form.role} onChange={e => setForm({ ...form, role: e.target.value })} data-testid="user-role-input"><option>SUPER_ADMIN</option><option>SALES_MANAGER</option><option>SALES</option></select></Field><Field label="Manager"><select className={selectClass} value={form.manager_id} onChange={e => setForm({ ...form, manager_id: e.target.value })} data-testid="user-manager-input"><option value="">— Tanpa manager —</option>{query.data?.items.filter(u => u.role === "SALES_MANAGER").map(u => <option key={u.id} value={u.id}>{u.name}</option>)}</select></Field><Field label="Telepon"><Input value={form.phone} onChange={e => setForm({ ...form, phone: e.target.value })} data-testid="user-phone-input" /></Field><Field label="Status"><select className={selectClass} value={form.status} onChange={e => setForm({ ...form, status: e.target.value })} data-testid="user-status-input"><option>Active</option><option>Inactive</option></select></Field><Field label="Password Awal"><Input type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} data-testid="user-password-input" /></Field><div className="flex justify-end gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={() => setModal(false)} data-testid="user-cancel-button">Batal</Button><Button type="submit" disabled={create.isPending} data-testid="user-save-button">Simpan</Button></div></form></Modal>}</div>;
}