import { Button } from "@/components/ui/button";

export interface Column<T> { key: string; label: string; render: (item: T) => React.ReactNode }

export default function DataTable<T extends { id: string }>({ columns, items, loading, empty, testId, total, page = 1, pageSize = 25, onPage }: { columns: Column<T>[]; items: T[]; loading?: boolean; empty?: string; testId: string; total?: number; page?: number; pageSize?: number; onPage?: (page: number) => void }) {
  return <div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm" data-testid={testId}><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-[11px] uppercase tracking-wider text-slate-500"><tr>{columns.map(column => <th key={column.key} className="whitespace-nowrap px-4 py-3 font-semibold">{column.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{loading ? <tr><td colSpan={columns.length} className="px-4 py-12 text-center text-slate-400">Memuat data...</td></tr> : items.length ? items.map(item => <tr key={item.id} className="hover:bg-slate-50" data-testid={`${testId}-row-${item.id}`}>{columns.map(column => <td key={column.key} className="whitespace-nowrap px-4 py-3">{column.render(item)}</td>)}</tr>) : <tr><td colSpan={columns.length} className="px-4 py-12 text-center text-slate-400">{empty ?? "Belum ada data"}</td></tr>}</tbody></table></div>{total !== undefined && <div className="flex items-center justify-between border-t border-slate-200 px-4 py-3 text-xs text-slate-500"><span>Menampilkan {items.length} dari {total} data</span>{onPage && <div className="flex items-center gap-2"><Button variant="outline" size="sm" disabled={page <= 1} onClick={() => onPage(page - 1)} data-testid={`${testId}-previous-page`}>Sebelumnya</Button><span>{page}</span><Button variant="outline" size="sm" disabled={page * pageSize >= total} onClick={() => onPage(page + 1)} data-testid={`${testId}-next-page`}>Berikutnya</Button></div>}</div>}</div>;
}