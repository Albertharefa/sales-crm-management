SALES CRM MANAGEMENT - DEPLOY FIX

Perbaikan utama:
1. models/crm.py dipusatkan dan dibuat bebas circular import.
2. Ditambahkan schema yang sebelumnya hilang:
   OptionsResponse, SalesTeamMetric, Opportunity, OpportunityCreate,
   PurchaseOrder, PurchaseOrderCreate, serta schema pendukung dashboard/upload.
3. Field frontend diselaraskan:
   Customer, Product, Quotation, Purchase Order, Opportunity, Activity/Task.
4. Customer API sekarang benar-benar server-side pagination + search dan
   mengembalikan {items,page,page_size,total}.
5. Validasi create customer/product/opportunity/order/quotation diperbaiki.
6. Quotation menghitung subtotal, discount, tax, dan grand_total di server.
7. Purchase Order menghitung total di server.
8. UploadResponse diselaraskan dengan response upload frontend.
9. UserPublic tidak lagi membawa password_hash ke response.
10. Semua router yang sebelumnya gagal import dapat dimuat oleh server.

File utama yang diperbarui:
- backend/models/crm.py
- backend/routers/customers.py
- backend/routers/products.py
- backend/routers/pipeline.py
- backend/routers/orders.py
- backend/routers/quotations.py

Validasi lokal:
- Semua file Python berhasil compile.
- Semua import dari models.crm berhasil ditemukan.
- CustomerCreate sekarang menolak payload {} dengan 422.
- Field date Pydantic diperbaiki agar tidak menjadi NoneType akibat
  konflik nama field "date" dengan from __future__ annotations.

Deployment:
Commit/push isi project ini ke branch yang dipakai Railway.
Setelah deploy, cek Deploy Logs dan Swagger. Target startup:
CRM SALES MANAGEMENT API STARTING
MongoDB connection OK
CRM API READY
Application startup complete.


FINAL PRODUCTION PACKAGING (2026-09-03)
---------------------------------------
- Added root Dockerfile with Node frontend build + Python FastAPI runtime.
- Added railway.json with Dockerfile builder and /health healthcheck.
- Added .dockerignore and .env.example.
- FastAPI now serves frontend/dist at / and React SPA routes via a final catch-all.
- All CRM routers are available under /api/... for the frontend.
- Legacy unprefixed routes remain available for compatibility.
- Swagger remains /docs and health remains /health.

2026-09-03 Production hardening:
- FastAPI accepts both canonical routes and frontend /api/* calls through internal prefix compatibility.
- React/Vite dist is served by FastAPI on the same Railway origin, including SPA deep-link fallback.
- MongoDB indexes are initialized at startup.
- Sessions use MongoDB TTL + explicit age validation.
- Admin password is no longer overwritten on every restart; explicit ADMIN_FORCE_PASSWORD_RESET=true is required.
- CORS is restricted to FRONTEND_ORIGINS in development/integrations rather than wildcard credentials.
- COOKIE_SECURE can be disabled for local HTTP development.


FINAL HARDENING — 2026-09-03
----------------------------
- Fixed the production API contract: frontend /api/* routes are now served by FastAPI while legacy unprefixed routes remain available.
- Production root now serves the React/Vite SPA instead of only returning the API status JSON.
- Added SPA deep-link fallback and safe static-file resolution.
- Added MongoDB indexes for common CRM queries plus session TTL expiration.
- Session cookies respect COOKIE_SECURE and server-side session age is validated.
- Existing admin passwords are preserved on restart; ADMIN_FORCE_PASSWORD_RESET=true is required for deliberate rotation.
- CORS no longer uses wildcard origins with credentials; configure CORS_ORIGINS only when cross-origin access is required.
- Added /api/health alongside /health for frontend/integration compatibility.
- Added Railway healthcheckPath=/health.
